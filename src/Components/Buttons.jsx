const Buttons = ({name}) => {
    return (
        <div className="">
            <button className="border border-black py-0.5 px-3 rounded-lg bg-gray-200 hover:bg-gray-100">{name}</button>
        </div>
    );
};

export default Buttons;
